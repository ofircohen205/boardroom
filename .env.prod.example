# Production Environment Configuration Template
# Copy this to .env.prod and fill in actual values

# Backend URL (used by frontend)
# For production deployment, set this to your actual backend URL
BACKEND_URL=https://api.yourdomain.com
BACKEND_WS_URL=wss://api.yourdomain.com/ws

# CORS Origins (backend)
# Comma-separated list of allowed origins for CORS
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# JWT Secret (REQUIRED) - Generate with: openssl rand -hex 32
# This is used to sign authentication tokens
JWT_SECRET=

# Database
# PostgreSQL connection string for the production database
DATABASE_URL=postgresql+asyncpg://boardroom:your-secure-password@db:5432/boardroom

# Database Password (used by Docker Compose)
DB_PASSWORD=your-secure-password

# LLM Provider Selection
# Options: anthropic, openai, gemini
LLM_PROVIDER=anthropic

# LLM Provider API Keys
# At least one is required depending on LLM_PROVIDER setting
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=

# Market Data Provider
# Options: yahoo, alpha_vantage
MARKET_DATA_PROVIDER=yahoo
ALPHA_VANTAGE_API_KEY=

# News/Social Search API
# Required for sentiment analysis
EXA_API_KEY=

# Redis Cache (optional)
# Falls back to in-memory cache if not configured
REDIS_URL=redis://redis:6379/0

# API Key Encryption
# 32-byte hex-encoded secret key for encrypting user API keys
# Generate with: openssl rand -hex 32
API_KEY_ENCRYPTION_KEY=
