# Production Docker Compose
services:
  backend:
    build:
      context: ..
      dockerfile: backend/docker/Dockerfile.prod
    expose:
      - "8000"
    environment:
      - DATABASE_URL=postgresql+asyncpg://boardroom:${DB_PASSWORD}@db:5432/boardroom
      - REDIS_URL=redis://redis:6379/0
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
      - EXA_API_KEY=${EXA_API_KEY}
      - ALPHA_VANTAGE_API_KEY=${ALPHA_VANTAGE_API_KEY}
      - JWT_SECRET=${JWT_SECRET}
      - CORS_ORIGINS=${CORS_ORIGINS:-http://localhost:5173}
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

  frontend:
    build:
      context: ../frontend
      dockerfile: docker/Dockerfile.prod
    ports:
      - "80:80"
    environment:
      - VITE_API_URL=${BACKEND_URL:-http://backend:8000}
      - VITE_WS_URL=${BACKEND_WS_URL:-ws://backend:8000/ws}
    depends_on:
      - backend
    restart: unless-stopped

  db:
    image: postgres:16-alpine
    environment:
      - POSTGRES_USER=boardroom
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_DB=boardroom
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U boardroom"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
    command: redis-server --appendonly yes
    restart: unless-stopped

volumes:
  postgres_data:
  redis_data:
